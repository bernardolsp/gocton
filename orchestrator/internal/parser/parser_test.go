package parser

import "testing"

func TestParseTaskFile(t *testing.T) {
	// The parser needs to, at minimum:
	// - Read the file
	// - Parse the file into a TaskFile struct
	// - Return the TaskFile struct
}
